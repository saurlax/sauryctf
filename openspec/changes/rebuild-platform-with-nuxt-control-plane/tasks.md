## 1. 固化首期契约与边界

- [x] 1.1 从现有 smoke flow、OpenAPI 和测试提取 Jeopardy 行为参考，逐项标注“纳入新契约”或“明确废弃”，并通过文档审查确认没有隐式兼容承诺
- [x] 1.2 建立首期能力清单和自动检查，验证 OpenAPI、数据库迁移、前端路由和 Worker 任务中不存在 AWD、VPN、终端或 Checker 实现
- [x] 1.3 定义新的稳定错误结构、分页规范和资源版本规则，并用契约测试验证 `code`、`message`、`request_id` 和字段错误
- [x] 1.4 固定 Node LTS、Go 版本和依赖锁文件策略，验证 CI 使用指定版本完成安装、类型检查、构建和测试

## 2. 建立控制面运行基础

- [x] 2.1 将 Nuxt 调整为可部署 Nitro 服务并保留适合预渲染的公开页面，验证生产构建可独立启动且动态 API 可访问
- [x] 2.2 建立 `server/api`、`server/domains`、`server/infrastructure`、`shared/contracts` 和 `db/migrations` 边界，并用依赖检查阻止页面与 Handler 直接访问数据库表
- [x] 2.3 加入 PostgreSQL、Redis、S3 兼容对象存储和本地邮件接收器的开发编排，验证一条命令可启动全部依赖
- [x] 2.4 配置 Drizzle、PostgreSQL 连接池、显式迁移命令和测试数据库生命周期，验证空库升级和重复升级
- [x] 2.5 建立生产配置 schema 与 Secret 引用，验证缺失 `NUXT_SESSION_PASSWORD`、数据库或对象存储配置时控制面不能进入 ready
- [x] 2.6 实现 request id、统一错误、日志 redaction 和输入体积限制中间件，验证畸形 JSON 与敏感字段不会污染日志
- [x] 2.7 建立共享 Zod 契约到 OpenAPI 和前端类型的生成链，验证无源变更时生成结果保持 clean
- [x] 2.8 整理 monorepo：将 Nuxt/Nitro 控制面迁入 `apps/web`，将新的 Go 实例 Worker 收敛到 `apps/worker`，把旧 Go 公网单体隔离到 `legacy/go-monolith`，并更新 workspace、脚本、CI、生成路径和依赖边界检查

## 3. 建立新的 PostgreSQL 权威模型

- [x] 3.1 创建用户、scrypt 凭证、邮箱验证/找回令牌、`session_version` 和全局角色表，验证用户名/邮箱唯一且不存在 Session 表
- [x] 3.2 创建站内通知、邮件投递和 domain outbox 表，验证业务事件与通知创建可在同一事务提交且消费幂等
- [x] 3.3 创建队伍、成员和邀请表，使用并发测试验证单用户单队伍、单队长和邀请码轮换约束
- [x] 3.4 创建 Jeopardy 比赛、分组、参赛、公告和公开时间线表，验证发布状态、UTC 阶段、参赛唯一和事件唯一约束
- [x] 3.5 创建题库模板、不可变版本、比赛题目快照、提示和附件引用表，验证分类约束拒绝 `awd`
- [x] 3.6 创建提交、正式/练习解、计分调整、反作弊线索和排行榜版本/快照表，验证同队同比赛题同模式解题唯一
- [x] 3.7 创建实例、`instance_jobs`、任务尝试和 Worker 观察字段，验证任务操作枚举拒绝 Checker、VPN、终端和通用代码执行
- [x] 3.8 创建内容对象、Writeup 版本、导入导出、平台设置和不可变审计表，验证引用完整性、追加事件和类型化设置约束
- [x] 3.9 建立 UUID、UTC 时间、金额外分数和版本号公共类型，使用 TypeScript/Go 契约夹具验证往返无损

## 4. 实现 nuxt-auth-utils 身份安全

- [x] 4.1 安装配置 `nuxt-auth-utils` 并声明最小 Session 类型，验证 Cookie 为 HttpOnly、生产 Secure、SameSite=Lax 且载荷不含角色、邮箱或凭证
- [x] 4.2 实现 scrypt 注册、登录和登录时摘要参数升级，验证正确密码、错误密码、参数升级和日志脱敏
- [x] 4.3 实现受保护请求的用户状态、邮箱状态和 `session_version` 校验，验证封禁、删除或版本不一致会清除当前 Cookie
- [x] 4.4 实现改密和找回密码事务，验证成功后递增 `session_version` 并使其他设备旧 Cookie 失效
- [x] 4.5 实现邮箱验证、重发和防枚举找回接口，验证令牌摘要、单次使用、过期和统一外部响应
- [x] 4.6 实现未验证账号能力门槛，验证其只能浏览公开内容、维护账号、重发验证和登出
- [x] 4.7 实现 Origin、CSRF 和按 IP/用户/动作分层限流，验证跨站 Cookie 写请求和爆破请求被拒绝
- [x] 4.8 实现可配置 Turnstile provider，验证启用时服务端校验、失败时拒绝，以及未配置时平台仍可通过本地限流运行
- [x] 4.9 实现 `user`、`organizer`、`admin` 全局能力矩阵，验证 organizer 可管理全部比赛且不存在比赛级 role binding
- [x] 4.10 实现空库 `admin / sauryctf` 引导，验证只有用户表为空时创建、未改密或未验证邮箱时只能访问账号维护、改密后旧 Cookie 失效
- [x] 4.11 实现安全站内通知和邮件 Outbox，验证邮件故障不回滚改密/找回事实且恢复后可幂等重试
- [x] 4.12 迁移登录、注册、登出、当前用户、账号维护和用户管理页面到新 API，运行 auth redirect、typecheck 和端到端身份测试

## 5. 实现队伍与 Jeopardy 比赛领域

- [x] 5.1 实现创建队伍、邀请码加入、退出、移除成员、轮换邀请和转让队长事务，运行并发成员变更测试
- [x] 5.2 实现队伍比赛锁定与管理员带原因纠正，验证已接受且未结束比赛阻止普通成员变更并写审计
- [x] 5.3 实现报名状态机、自动通过、人工审核、撤回和分组分配，验证接受时重新检查邮箱、人数及比赛条件
- [x] 5.4 实现比赛草稿、发布、归档和 UTC 阶段派生，验证跨越开始/结束时间时所有领域服务得到同一阶段
- [x] 5.5 实现比赛时间、可见性、邀请码、报名策略、练习、封榜和 Writeup 配置校验，运行字段级失败场景测试
- [x] 5.6 在比赛 API、导入和数据库边界拒绝 AWD 与未知赛制，验证失败请求不创建比赛或运行资源
- [x] 5.7 实现公告创建、修改、撤回和定时发布，验证未发布时间内容不可见且 outbox 事件可去重
- [x] 5.8 实现公开比赛时间线，验证只发布公告、题目/提示发布、首解、封榜和阶段变化，管理事件只进入后台
- [x] 5.9 实现比赛发布前完整性检查，验证题目、Flag、附件和动态实例配置错误可定位到具体资源

## 6. 实现题库、提交与确定性计分

- [x] 6.1 实现题库模板与不可变版本维护，验证更新说明或附件会创建新版本且不覆盖历史
- [x] 6.2 实现挂题快照和公平性字段修订命令，验证模板更新不会改变已发布比赛且紧急修订产生版本和审计
- [x] 6.3 实现 `web`、`pwn`、`crypto`、`reverse`、`misc`、`forensics` 分类与正交 Flag/计分/实例策略校验，验证 `awd` 分类被拒绝
- [x] 6.4 实现选手题目投影，验证参赛状态、比赛阶段、发布时间和提示时间控制受保护内容且绝不返回 Flag 材料
- [x] 6.5 实现静态 Flag、每队派生 Flag 和受控同步校验器，验证常量时间比较、格式、密钥版本和选手响应脱敏
- [x] 6.6 实现提交资格与细粒度限流，验证未验证邮箱、未接受、未开赛、已截止、超次数和封禁请求不泄露 Flag 正确性
- [x] 6.7 实现追加提交事实与敏感答案摘要/加密策略，验证普通管理查询只显示遮罩值且日志无答案
- [x] 6.8 实现正式解唯一事务和稳定首解次序，使用并发测试验证同队重复正确提交只创建一个解且多队只有一个 first
- [x] 6.9 实现 `fixed-v1` 和 `decay-v1` 计分器及显式调整记录，使用固定夹具验证公式、边界和重放
- [x] 6.10 实现正式与练习隔离，验证练习解不改变正式解数、动态分值、首解或排行榜版本
- [x] 6.11 实现反作弊线索及人工复核状态，验证异常重复答案只产生证据线索而不自动处罚
- [x] 6.12 编写事实重放器，验证相同提交、解题和调整事实重复重放产生相同题值、总分、首解与排名摘要

## 7. 实现排行榜、缓存与实时事件

- [x] 7.1 实现总榜与分组榜构建器，验证总分降序、最后计分时间升序和稳定 ID 兜底排序
- [x] 7.2 实现公共封榜快照和 organizer/admin 实时榜，验证封榜后提交只改变内部视图直到公开结算
- [x] 7.3 实现包含 schema、比赛、视图、分组和版本的 Redis cache-aside，验证错误角色或旧版本缓存不会命中
- [x] 7.4 实现 outbox dispatcher、事件去重和缓存版本失效，验证发布失败可重试且不会重复推进业务事实
- [ ] 7.5 实现排行榜 single-flight、短锁和 PostgreSQL 持久快照降级，验证 Redis 断开时不会逐请求全量查询
- [ ] 7.6 实现带稳定事件 ID、恢复窗口和 reset 语义的 SSE，验证 `Last-Event-ID` 可补发或触发完整刷新
- [ ] 7.7 验证 Redis 被清空时可重建公开列表、排行榜和实时状态，且权限、成绩和实例所有权保持不变

## 8. 抽取 Go 实例 Worker

- [ ] 8.1 新建 `apps/worker/cmd/worker` 入口及独立配置、数据库角色和健康检查，验证 Worker 不注册任何公网业务路由
- [ ] 8.2 定义 `instance_jobs` 版本化载荷与 TypeScript/Go JSON 夹具，验证 ensure、inspect、destroy、reconcile 双向兼容并拒绝未知操作
- [ ] 8.3 实现 `SKIP LOCKED` 批量领取、租约续约、fencing token 和优雅停止，使用两个 Worker 并发验证单次有效完成
- [ ] 8.4 实现任务尝试、错误分类、指数退避、取消、superseded 和死信，使用故障注入验证每种最终状态
- [ ] 8.5 实现实例期望代次与观察状态条件更新，验证过期 Worker 和旧代次任务不能覆盖新观察结果
- [ ] 8.6 实现周期 Reconciler 和孤儿报告，验证只管理完整平台标签资源且未知资源不被删除
- [ ] 8.7 删除 Go Worker 对用户、队伍、比赛、Flag 和计分表的数据库权限，使用越权 SQL 集成测试验证访问失败

## 9. 实现 Docker 与 Kubernetes Provider

- [ ] 9.1 定义 `Ensure/Inspect/Destroy` Provider 契约和确定性资源命名，使用共享契约测试验证缺失资源销毁幂等
- [ ] 9.2 使用 Docker Engine API 实现镜像、环境、资源限制、端口和标签管理，验证不通过 shell 拼接命令
- [ ] 9.3 使用 `client-go` 实现 Kubernetes workload、Service、Secret 和就绪观察，运行 envtest 或测试 k3s 集成测试
- [ ] 9.4 实现 Gateway API/Ingress HTTP 入口和受控 TCP 入口适配，验证仅在 workload 与路由 Ready 后回写入口
- [ ] 9.5 实现非 root、安全上下文、禁用 ServiceAccount token、资源配额和 NetworkPolicy 默认值，使用资源快照验证危险配置被拒绝
- [ ] 9.6 实现动态 Flag 敏感载荷信封加密、注入和完成后清除，验证日志、普通任务查询和资源标签不包含 Flag
- [ ] 9.7 注入创建后回写前崩溃、重复启动、Provider 超时和资源被外部删除，验证 Reconcile 收敛且不产生重复资源

## 10. 实现内容、设置、后台与可观测性

- [ ] 10.1 实现 S3 内容对象、临时上传、摘要提交和 24 小时无引用回收，验证共享引用不会误删且控制面重启后文件可用
- [ ] 10.2 实现授权下载和短期签名 URL，验证私有题目、Writeup 和危险媒体类型的权限与 disposition
- [ ] 10.3 实现版本化 Writeup 保存、提交、截止、审核和批量导出，运行截止前后及管理员纠正场景测试
- [ ] 10.4 实现版本化 Jeopardy 比赛包导入导出和原子导入，使用 AWD 配置、路径穿越、压缩炸弹和摘要错误夹具验证安全拒绝
- [ ] 10.5 实现品牌、Logo、主题、默认语言、公开注册和首期密码登录设置，验证 Secret 无法写入数据库且未实现 OIDC 模式被拒绝
- [ ] 10.6 实现中文与英文系统 UI、状态和邮件模板，验证切换语言不修改或伪造题目、公告和比赛内容
- [ ] 10.7 实现不可变审计并覆盖用户、角色、比赛、题目、成绩、实例和设置写操作，验证事务失败不留下成功审计
- [ ] 10.8 实现结构化日志、OpenTelemetry 和核心指标，运行实例启动验证 request/job/instance 关联完整且无敏感值
- [ ] 10.9 实现管理监控与邮件投递页面，验证提交、线索、实例、任务、通知、Writeup 和审计可筛选且陈旧观察被标记
- [ ] 10.10 实现缓存重建、死信重放、实例对账、Session 版本失效和结果重算命令，验证权限、原因、幂等和审计
- [ ] 10.11 配置审计一年、安全日志 90 天和正式赛事实长期保留策略，使用时间推进测试验证到期清理不删除权威成绩

## 11. 首期发布验收与旧 Go 业务退役

- [ ] 11.1 在全新 PostgreSQL 环境运行完整 Jeopardy smoke flow，验证默认管理员改密、注册验证、建队、报名、挂题、发布、提交、排行榜、练习和 Writeup
- [ ] 11.2 在真实 Docker 和测试 k3s 运行实例生命周期套件，验证创建、就绪、续期、配额、销毁、过期和崩溃恢复
- [ ] 11.3 执行认证、角色越权、CSRF、Turnstile、上传、ZIP、Flag 泄露和 Worker RBAC 安全测试并修复全部高风险结果
- [ ] 11.4 执行 300 队、1000 并发、1000 实例和 200 提交/秒短峰值压测，验证普通提交 p95 小于 300ms且排行榜 5 秒内更新
- [ ] 11.5 执行 Redis、Worker、Kubernetes API、对象存储、邮件服务和单控制面副本故障演练，验证规范中的降级与恢复
- [ ] 11.6 执行 PostgreSQL 与对象存储备份恢复，验证 RPO 5 分钟、RTO 30 分钟、附件摘要和排行榜重建目标
- [ ] 11.7 运行仓库级 AWD 禁入检查，验证数据库、OpenAPI、前端路由、Worker 任务和部署清单均无 AWD 实现
- [ ] 11.8 删除 `legacy/go-monolith` 及所有 Go 公网 Handler、Auth 和业务 Service 引用，验证 `apps/worker` 只产生私有 Worker 且浏览器只能访问 `apps/web`
- [ ] 11.9 更新部署、运维、应急与开发文档，验证新成员能仅按文档启动空环境并完成 Jeopardy smoke flow
- [ ] 11.10 执行 OpenSpec、OpenAPI、类型检查、Go 测试、Nuxt 构建和端到端套件，保存首期发布验收报告并阻止未达指标的发布

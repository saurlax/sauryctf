# 登录与注册

当前前端已经提供独立的 `/login` 与 `/register` 页面，右上角导航也固定提供两个入口：

- `登录`
- `注册`

## 默认管理员

只有在 `users` 表完全为空时，后端启动才会自动创建默认管理员：

- 用户名：`admin`
- 密码：`sauryctf`

一旦数据库里已经有任意用户：

- 后端不会再补建 `admin`
- 首页和 `/login` 也不会继续把默认口令当成常规入口提示

## 普通用户链路

- 直接访问 `/register` 注册成功后，会自动登录并进入 `/console?onboarding=team`
- 控制台首页会立刻提示先创建或加入队伍，避免新用户刚注册完还不知道下一步该做什么
- 如果是从比赛详情页跳到 `/register?redirect=/games/{id}`：
  - 注册成功后会先进入 `/console/team?redirect=/games/{id}`
  - 创建队伍或加入队伍成功后，会自动回到原比赛

## 登录链路

- `/login` 支持用户名或邮箱登录
- 如果当前链接带有 `redirect` 查询参数，登录成功后会优先跳回该地址
- 如果没有 `redirect`，默认进入 `/console`

## 推荐联调顺序

1. 空库启动后先用 `admin / sauryctf` 登录
2. 创建一场公开比赛并激活
3. 再注册普通账号
4. 创建或加入队伍
5. 回到比赛详情继续报名、提 Flag、看榜

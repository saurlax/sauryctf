# 管理端

当前管理端先实现了一个最小闭环页面：

- 路径：`/console/admin`
- 仅 `admin` / `super_admin` 角色可见
- 能力：
  - 创建比赛
  - 创建题目
  - 将已有题目挂载到比赛

## 当前设计原则

- 先复用现有后端 admin API
- 前端保持极简，只做表单输入和结果反馈
- 同步加载比赛、题目列表，方便管理时直接选择
- 不在这一版里加入复杂的编辑、删除、批量操作

## 后续可继续补充

- 比赛列表与编辑
- 题目列表与编辑
- 队伍审核、公告管理、容器管理
